const API = {
  BASE_URL: 'https://swapi.dev/api/',
  ENTITIES_PATH: ['planets', 'films', 'species', 'vehicles', 'starships'] as const
};

const BASE_SPACING = '1rem';
const BREAKPOINT_MOBILE = '640px';

export { API, BASE_SPACING, BREAKPOINT_MOBILE };
